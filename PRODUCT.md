# AI Quota 产品范围

用户确认的产品范围：在独立 Mac App 中同时提供菜单栏状态和桌面悬浮窗，展示 Codex 编程额度、Cursor 个人订阅额度、Claude Pro / Max 订阅额度及 Gemini 网页/App 的 Google AI 个人订阅额度。

- 菜单栏同时显示 Codex 5 小时/每周、Cursor Models/Other Models ，以及 Claude、Gemini 5 小时/每周剩余百分比。
- 悬浮窗显示进度条、Codex 重置倒计时、Cursor 官方账期日期和更新时间。
- 支持拖动、位置恢复、隐藏/恢复、可选置顶。
- 两个视图共享数据；每分钟刷新、手动刷新、唤醒刷新。
- 四家共用顶部“全局关注”开关：开启即刷新，此后每 10 秒刷新，默认 10 分钟后自动停止；更多操作菜单可选择并记住 1、5、10、30 分钟，修改从下次开启生效，可随时手动停止；停止后恢复普通刷新。进行中的请求不叠加、不强制取消。
- 菜单栏弹窗及悬浮窗按所在屏幕限制大小和位置，内容较长时滚动查看。
- 未连接、加载中、失败、过期明确展示，禁止假数据或将缺失额度当作 100%。
- 首次 Cursor 登录在 App 内官方页面完成；凭据不经本应用代码读取。
- 不含付费重置、订阅调整、账单修改、开机启动、系统通知或云端服务。

## Quota change feedback

- Successful refreshes that change the displayed whole-number remaining percentage trigger animation and sound.
- Changes below 3 percentage points use a subtle pulse; 3–19 points use a stronger ring/sweep; 20+ points use a larger particle burst lasting four seconds.
- Increases use mint, decreases use soft red. A temporary signed percentage-point badge identifies the direction and amount. The menu-bar label also pulses.
- Codex and Cursor compare their own live baselines. First load, unchanged displayed percentages, missing/replaced quota windows, and a gap over 150 seconds stay silent.
- A reset that actually restores remaining quota can trigger a large positive effect.
- Sound is enabled by default and can be muted in More. Three directly clickable menu items preview all three levels without changing real quota values.
- Respect macOS Reduce Motion: use a subdued highlight and disable moving particles / animated value transitions.

音效采用原创柔和钟琴旋律：小变化两音、明显变化三音、大变化五音；增加上行、减少下行，强度越高越清晰。通过更多菜单的预览入口试听。

更多菜单可直接选择并记住“经典钟琴”或“Handpan 合奏”，默认 Handpan 合奏，静音开关对两套都生效。合奏采用确认过的第三版试听音频；明显变化门槛为 3 个百分点。大幅恢复旋律仅用于增加至少 20 点；大幅减少使用中档合奏，避免播放庆祝音效。

## Claude 与独立折叠

四家卡片均支持独立折叠，状态持久化并共享于菜单弹窗/桌面悬浮窗。默认展开；折叠时仍显示额度摘要和错误，顶部保留全局关注开关，后台刷新、音效和关注计时不变。Claude 在 App 内官方网页登录，显示实际返回的 5 小时、周、Sonnet/Opus 周额度及重置时间；缺失窗口不补成 100%。多工作区账号需在更多菜单明确选择工作区。Claude 支持全局关注、缓存和变化反馈。

窗口高度跟随展开、折叠及内容变化，保留正常内边距；仅超过屏幕可用高度时滚动。更多 → 调整卡片顺序，使用上移/下移按钮，完成后点击“完成排序”。顺序持久化，桌面窗口、菜单弹窗和菜单栏摘要共用；排序不改变各家的折叠状态、登录或关注计时。

## 24 小时趋势与周期预测

- 每个指标以余量下方小字显示过去 24 小时累计消耗，点击进度条展开周期累计已用趋势与预测线（额度百分点，非 token 数量）。历史从启用开始在本机积累，保留最近 48 小时高密度历史和更早的当前周期历史（15 分钟采样，每指标最多 40000 点）；数据不足显示已记录时长，重置、修正及超过 150 秒的缺口断线。没有第二个样本时不显示虚假的零用量。
- 累计消耗只统计可识别的增长；跨周期加上已知新周期用量，旧周期未观测到的尾部标记缺口。24 小时边界仅在同周期且连续的相邻样本间线性估算，显示“约”。离线期间同周期端点差值可计入累计，但趋势不填补中间走势。
- 平均使用速度 = 本周期累计已用 / 从周期开始至最新成功样本的时间；预计耗尽时间 = 样本时间 + 剩余 / 平均速度。不能用首次打开时间替代周期起点。无可靠周期元数据、已到重置时间或数据过期时不预测；有效周期内零用量显示暂无消耗。
- 紧急度按预计可用时长 / 距离重置时长连续着色：至少 1.25 为绿，1.0 为黄，0.75 为浅红，0.35 及以下为深红，中间插值。剩余段和预测文字使用风险色；更早已用为暗灰蓝，近24h已用为稍亮低饱和雾蓝，实际趋势线使用中性色；保留数值及文字说明。
- 使用 416pt 宽和按内容自动调整高度；超过屏幕高度滚动。Gemini 在 App 的 Google 官方页面登录，显示实际返回的 5 小时及每周额度，不导入 Chrome 凭据，不以 API/CLI 额度替代。

## 紧凑面板与全局关注（2026-09-11）

用户批准的范围：四家共用关注开关、低亮度已用分段和按风险着色的剩余段、默认隐藏图表、点击指标进度条展开图表、明确定位额度变化。保留音色、音量开关、独立折叠、排序、登录与错误缓存。

- 全局关注共享启动/停止时刻，立即刷新四家，再以各自不重叠请求的10秒调度运行；时长沿用已有选择，默认10分钟，菜单可改1/5/10/30分钟，下次开启生效。到期或手动停止恢复普通刷新。
- 变化保留30秒具体指标高亮，固定尺寸的余量下方小字由24h用量临时替换为增减百分号标签，折叠时仍能辨认提供商；短动画按1–2、3–19、20+点分档，遵守Reduce Motion。预览明确标识，不修改真实额度。
- 进度条总长100%，顺序为剩余、当前周期近24h已用、更早已用。24h小字可跨周期超过100点，进度条分段不跨周期。没有足够历史时只显示已知部分，说明历史不足；过期或无法预测时剩余段为白色。
- 周期均速优先；长于5小时的周期前24小时（5小时及以下周期前5分钟）或缺少起点时，有至少60秒有效历史则使用近24h已记录消耗/实际记录时长估算，标明依据与覆盖时长。没有reset或数据过期不预测。前5分钟无历史时可用有效周期均速；两者都无依据则不预测。
- 图表覆盖当前周期，并可保留近24h的上一周期历史；实线为观测，重置/修正/缺口断线，虚线从最新样本按均速画到重置或耗尽。缺失历史不补画。较早历史按15分钟取样，旧版本已丢弃数据不能追溯恢复。
- 窗口按内容伸缩；超过所在屏幕可用高度时滚动。默认所有图表收起，展开状态仅保留当前视图生命周期。


## 稳定变化提示与关注开关声音

取消顶部通知，无论同时有多少指标变化都不增加行高。指标背景高亮不透明度提升到25%，左侧色条4pt；30秒内余量下方固定112×13pt区域显示“↓ 3% · 刚刚”，到期恢复24h用量，悬停可查看原用量和“百分点”解释。百分号为统一UI简写，计算仍是额度百分点差而非相对变化率。卡片折叠时标题显示标记，不自动展开。

底部固定“最近变化”按钮亮起提示新事件，点击独立浮层展示本次运行最近30条指标记录（不持久化）；浮层固定300×240pt，内容滚动，不改变主窗口尺寸。

进度条从左到右为鲜明剩余段、雾蓝近24h已用段、暗灰蓝更早已用段。颜色风险计算不变。

手动开启全局关注播放三次相同的低音调秒针声，间隔150ms，使用确认的低音调v9（振幅再降低30%）；提前关闭播放自锁按钮弹出声。两者使用用户确认的低音量试听原文件。自动到期不播放开关音。开关声独立于额度旋律播放，服从现有静音开关；不更改已确认的两套额度音色。

成功刷新发布新额度前同步界面时钟，避免新样本暂时晚于界面时钟造成预测失效/进度条变灰。刷新中保留原数据；真实失败、过期、缺少周期信息继续明确显示。

## 界面语言

更多 → 语言可选择简体中文或 English，即时应用并记住选择。未选择时，中文系统默认中文，其他系统默认英文。语言仅影响本机界面、菜单、提示、图表与辅助功能标签；不改变提供商数据、额度标识、历史和登录。官方登录网页及系统错误遵循各自语言设置。

## Close Watch mascot

密切关注每次启动等概率选择 sad、呆萌、趴卡片三款猫之一，同次关注保持不变，悬浮窗与菜单弹窗共用选择。猫盖住 AI QUOTA，裁切于窗口外框。前两款沿身体中轴快速出入；趴猫脸颊压住首张卡片上沿，退场先进入卡片后方再收回。三款均有眨眼；减少动态效果时使用静止姿态。装饰不改变窗口高度、用量、刷新与声音。
